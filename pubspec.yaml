name: stylish_dialog
description: A collection of stylish animated dialogs like Normal, Progress, Success, Info, Warning, and Error for flutter.
version: 0.0.5+1
homepage: https://github.com/MarsadMaqsood/StylishDialogFlutter.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter